// {
//   "compilerOptions": {
//     "target": "es5", // Устанавливает версию ECMAScript, в которую компилируются TypeScript файлы. В данном случае - ES5.
//     "module": "commonjs", // Устанавливает тип модулей, которые генерирует TypeScript компилятор. В данном случае - commonjs.
//     "strict": true, // Включает строгие проверки типов TypeScript, что обеспечивает более безопасную разработку.
//     "jsx": "react", // Указывает TypeScript, как обрабатывать JSX синтаксис. В данном случае - для использования с React.
//     "esModuleInterop": true, // Облегчает работу с CommonJS модулями, используя ECMAScript модульную систему.
//     "resolveJsonModule": true, // Позволяет импортировать JSON файлы напрямую в коде TypeScript.
//     "baseUrl": "./src", // Определяет базовый путь для разрешения наборов импортов из исходных файлов.
//     "paths": {
//       "*": ["*", "src/*"] // Позволяет TypeScript разрешать модули, указывая пути относительно базового пути.
//     }
//   },
//   "include": ["src/**/*"] // Указывает TypeScript, какие файлы включать в процесс компиляции. В данном случае - все файлы в директории src.
// }

{
  "compilerOptions": {
    "target": "ES6", // Устанавливаем версию ECMAScript, к которой компилируются TypeScript файлы.
    "module": "CommonJS", // Указываем, какой тип модулей используем. В данном случае - CommonJS для Node.js совместимости.
    "jsx": "react-jsx", // Задаем подход для обработки JSX синтаксиса в React.
    "strict": true, // Включаем строгие проверки типов, чтобы обеспечить безопасность кода.
    "esModuleInterop": true, // Упрощает импорт модулей, написанных в стиле CommonJS, в TypeScript.
    "skipLibCheck": true, // Пропускаем проверку типов для библиотечных файлов типов, чтобы ускорить компиляцию.
    "forceConsistentCasingInFileNames": true, // Обеспечиваем согласованное использование регистра в именах файлов.
    "moduleResolution": "node", // Указываем способ разрешения модулей с использованием Node.js.
    "resolveJsonModule": true, // Разрешаем импорт JSON файлов напрямую из TypeScript.
    "baseUrl": ".", // Базовый каталог для разрешения модулей. Путь указывается относительно расположения файла tsconfig.json.
    "paths": {
      "*": ["*", "src/*"] // Маршруты для разрешения модулей. "*" указывает на любой модуль, "src/*" - на модули внутри папки src.
    }
  },
  "include": ["src/**/*"], // Указываем, какие файлы должны быть включены в процесс компиляции TypeScript.
  "exclude": ["node_modules"] // Указываем, какие файлы или папки нужно исключить из компиляции TypeScript.
}

